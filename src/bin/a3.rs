//Flow control using if ... else




fn main(){
    //Displays a message based on the value of boolean variable
    let my_bool  = true;
    //When the variable is set to true, display "hello"
    if my_bool == false {
        println!("Hello")
    } else {
        println!("Goodbye")
    }
}